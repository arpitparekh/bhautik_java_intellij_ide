package oopc;

public class Vito{
    int a;          // same file // child(same file)
    private int b;
    public int c;   // same file // child(same file)
    protected int d;// same file // child(same file)
}